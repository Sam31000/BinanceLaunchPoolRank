import { DisplayPricePipe } from './display-price.pipe';

describe('DisplayPricePipe', () => {
  it('create an instance', () => {
    const pipe = new DisplayPricePipe();
    expect(pipe).toBeTruthy();
  });
});
